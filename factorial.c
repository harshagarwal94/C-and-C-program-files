#include<stdio.h>
void main()
{
    int i,a,f=1,x;
     printf("enter the value to be tested ");
     scanf("%d",&a);
     if(a==0)
        printf("factorial=1");
    else
     {
       for(i=a;i>=1;i--)
       {
         f=f*i;
       }
       printf("the value is %d",f);
     }
}
printf("the total sum of the numbers you entered is %d",sum);
   printf("\n thanks for entering the no of integers\n now enter the numbers you want to    give as input");
   for(i=0; i<=n-1; i++)
{
